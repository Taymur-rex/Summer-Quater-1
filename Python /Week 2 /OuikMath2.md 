1.Define what a function is in your own words
2. What key word is used to define a function? ( A function is an coolable which mean it does it's job once. ) 
3. What does a return statement do? ( The return statement goes back when it fully completed. ) 
4. What is the difference between arguments and parameters? ( Parameters are the placeholders in the function,  
Arguments are the real value pass through when you call in the function. )